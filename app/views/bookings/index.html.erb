<div class="container mt-3">
  <h1> All your personnal bookings <h1>
<div>

  <% @bookings.each do |booking| %>
   <ul>
    <%#  <li> <%= booking.location %> </li>
      <li> <%= booking.friend.first_name %> <%= booking.friend.last_name %></li>
      <li> <%= booking.date %> </li>
      <li> <%= booking.friend.category %> </li>
    </ul>
  <% end %>

<%# <%= link_to "Go to your booked friend profile", friend_path %>
<%= link_to "Back to friends", friends_path %>
